# attchat-gateway-api

API Gateway cho hệ thống ATTChat

- Xác thực JWT
- Logging, CORS
- Health check
- Proxy các route /api/* tới backend chat-service
- Chuẩn bị sẵn middleware xác thực, rate limit, metrics
- Không xử lý WebSocket
